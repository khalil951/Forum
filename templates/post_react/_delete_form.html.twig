<form method="post" action="{{ path('app_post_react_delete', {'id': post_react.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_react.id) }}">
    <button class="btn">Delete</button>
</form>
